providers = ["python"]
pythonVersion = "3.11"

[phases.install]
cmds = [
  "pip install --no-cache-dir -r server_py/requirements.txt"
]

[start]
cmd = "cd server_py && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"


