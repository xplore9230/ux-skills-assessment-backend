# Database (Required for storing quiz data)
DATABASE_URL=postgresql://user:password@host:port/database

# Deployment
NODE_ENV=production
PORT=5000

# Python AI Service URL (Required for AI features - set after Railway deployment)
# This points to your Railway-deployed Python backend with Ollama/Llama
VITE_PYTHON_API_URL=https://your-railway-app.up.railway.app

# NOTE: We use Ollama (local LLM) via Python backend, so NO OpenAI or SERP API keys needed!
